export type controlPanelProps = {
    cssClass?: string;

};

export default function ControlPanel({ cssClass }: controlPanelProps) {
    return (
        <div className={cssClass}>
            
        </div>
    )
}